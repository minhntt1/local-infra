name: Liquibase Preview
on:
  pull_request:
    paths: ['database/**']
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  preview:
    runs-on: [self-hosted, k8s]
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: changes
        if: github.event_name != 'workflow_dispatch'
        with:
          filters: |
            dev-network-statistics:
              - 'database/dev/mysql/network_statistics/**'
            dev-quartz:
              - 'database/dev/mysql/quartz/**'
            dev-testdb:
              - 'database/dev/mysql/testdb/**'
            prod-network-statistics:
              - 'database/prod/mysql/network_statistics/**'
            prod-quartz:
              - 'database/prod/mysql/quartz/**'

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - uses: liquibase/setup-liquibase@v3
        with:
          version: '4.32.0'
          edition: community

      - name: Install MySQL JDBC driver
        run: |
          mkdir -p /tmp/liquibase-lib
          CONNECTOR="mysql-connector-j-9.1.0.jar"
          if [ ! -f "/tmp/liquibase-lib/$CONNECTOR" ]; then
            curl -fsSL -o "/tmp/liquibase-lib/$CONNECTOR" \
              "https://repo1.maven.org/maven2/com/mysql/mysql-connector-j/9.1.0/$CONNECTOR"
          fi
          echo "LIQUIBASE_CLASSPATH=/tmp/liquibase-lib/$CONNECTOR" >> "$GITHUB_ENV"

      - name: Generate preview report
        run: |
          rm -f /tmp/liquibase-preview.md
          touch /tmp/liquibase-preview.md

      - name: Record existing baselines (changelogSync, no SQL executed)
        run: |
          sync_db() {
            local url="$1" pass="$2" clog="$3"
            liquibase --url="$url" --username="root" --password="$pass" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="$clog" changelogSync || true
          }
          sync_db "jdbc:mysql://mysql-dev.dev.svc.cluster.local:3306/network_statistics?connectionTimeZone=UTC" "$DEV_JDBC_PASSWORD"  "database/dev/mysql/network_statistics/changelog/changelog1.xml"
          sync_db "jdbc:mysql://mysql-dev.dev.svc.cluster.local:3306/quartz?connectionTimeZone=UTC"            "$DEV_JDBC_PASSWORD"  "database/dev/mysql/quartz/changelog/changelog1.xml"
          sync_db "jdbc:mysql://mysql-dev.dev.svc.cluster.local:3306/testdb?connectionTimeZone=UTC"           "$DEV_JDBC_PASSWORD"  "database/dev/mysql/testdb/changelog/changelog1.xml"
          sync_db "jdbc:mysql://mysql-prod.prod.svc.cluster.local:3306/network_statistics?connectionTimeZone=UTC" "$PROD_JDBC_PASSWORD" "database/prod/mysql/network_statistics/changelog/changelog1.xml"
          sync_db "jdbc:mysql://mysql-prod.prod.svc.cluster.local:3306/quartz?connectionTimeZone=UTC"          "$PROD_JDBC_PASSWORD" "database/prod/mysql/quartz/changelog/changelog1.xml"

      - name: Preview dev - network_statistics
        if: github.event_name == 'workflow_dispatch' || steps.changes.outputs.dev-network-statistics == 'true'
        run: |
          JDBC_URL="jdbc:mysql://mysql-dev.dev.svc.cluster.local:3306/network_statistics?connectionTimeZone=UTC"
          {
            echo "## dev/network_statistics"
            echo ""
            echo "**Status:**"
            liquibase --url="$JDBC_URL" --username="root" --password="$DEV_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/dev/mysql/network_statistics/changelog/changelog1.xml" \
              status --verbose
            echo ""
            echo "<details><summary>SQL Preview</summary>"
            echo ""
            echo '```sql'
            liquibase --url="$JDBC_URL" --username="root" --password="$DEV_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/dev/mysql/network_statistics/changelog/changelog1.xml" \
              updateSQL
            echo '```'
            echo ""
            echo "</details>"
            echo ""
            echo "---"
          } >> /tmp/liquibase-preview.md

      - name: Preview dev - quartz
        if: github.event_name == 'workflow_dispatch' || steps.changes.outputs.dev-quartz == 'true'
        run: |
          JDBC_URL="jdbc:mysql://mysql-dev.dev.svc.cluster.local:3306/quartz?connectionTimeZone=UTC"
          {
            echo "## dev/quartz"
            echo ""
            echo "**Status:**"
            liquibase --url="$JDBC_URL" --username="root" --password="$DEV_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/dev/mysql/quartz/changelog/changelog1.xml" \
              status --verbose
            echo ""
            echo "<details><summary>SQL Preview</summary>"
            echo ""
            echo '```sql'
            liquibase --url="$JDBC_URL" --username="root" --password="$DEV_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/dev/mysql/quartz/changelog/changelog1.xml" \
              updateSQL
            echo '```'
            echo ""
            echo "</details>"
            echo ""
            echo "---"
          } >> /tmp/liquibase-preview.md

      - name: Preview dev - testdb
        if: github.event_name == 'workflow_dispatch' || steps.changes.outputs.dev-testdb == 'true'
        run: |
          JDBC_URL="jdbc:mysql://mysql-dev.dev.svc.cluster.local:3306/testdb?connectionTimeZone=UTC"
          {
            echo "## dev/testdb"
            echo ""
            echo "**Status:**"
            liquibase --url="$JDBC_URL" --username="root" --password="$DEV_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/dev/mysql/testdb/changelog/changelog1.xml" \
              status --verbose
            echo ""
            echo "<details><summary>SQL Preview</summary>"
            echo ""
            echo '```sql'
            liquibase --url="$JDBC_URL" --username="root" --password="$DEV_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/dev/mysql/testdb/changelog/changelog1.xml" \
              updateSQL
            echo '```'
            echo ""
            echo "</details>"
            echo ""
            echo "---"
          } >> /tmp/liquibase-preview.md

      - name: Preview prod - network_statistics
        if: github.event_name == 'workflow_dispatch' || steps.changes.outputs.prod-network-statistics == 'true'
        run: |
          JDBC_URL="jdbc:mysql://mysql-prod.prod.svc.cluster.local:3306/network_statistics?connectionTimeZone=UTC"
          {
            echo "## prod/network_statistics"
            echo ""
            echo "**Status:**"
            liquibase --url="$JDBC_URL" --username="root" --password="$PROD_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/prod/mysql/network_statistics/changelog/changelog1.xml" \
              status --verbose
            echo ""
            echo "<details><summary>SQL Preview</summary>"
            echo ""
            echo '```sql'
            liquibase --url="$JDBC_URL" --username="root" --password="$PROD_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/prod/mysql/network_statistics/changelog/changelog1.xml" \
              updateSQL
            echo '```'
            echo ""
            echo "</details>"
            echo ""
            echo "---"
          } >> /tmp/liquibase-preview.md

      - name: Preview prod - quartz
        if: github.event_name == 'workflow_dispatch' || steps.changes.outputs.prod-quartz == 'true'
        run: |
          JDBC_URL="jdbc:mysql://mysql-prod.prod.svc.cluster.local:3306/quartz?connectionTimeZone=UTC"
          {
            echo "## prod/quartz"
            echo ""
            echo "**Status:**"
            liquibase --url="$JDBC_URL" --username="root" --password="$PROD_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/prod/mysql/quartz/changelog/changelog1.xml" \
              status --verbose
            echo ""
            echo "<details><summary>SQL Preview</summary>"
            echo ""
            echo '```sql'
            liquibase --url="$JDBC_URL" --username="root" --password="$PROD_JDBC_PASSWORD" \
              --classpath="$LIQUIBASE_CLASSPATH" \
              --changelog-file="database/prod/mysql/quartz/changelog/changelog1.xml" \
              updateSQL
            echo '```'
            echo ""
            echo "</details>"
          } >> /tmp/liquibase-preview.md

      - name: Post preview as PR comment
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = '/tmp/liquibase-preview.md';
            if (!fs.existsSync(path)) {
              core.notice('No preview report generated (no database files changed)');
              return;
            }
            const body = fs.readFileSync(path, 'utf8').trim();
            if (!body) return;
            const pr = context.payload.pull_request;
            if (pr) {
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: pr.number,
                body: body
              });
            } else {
              core.summary.addRaw(body);
              await core.summary.write();
            }
