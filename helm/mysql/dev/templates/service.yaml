apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-dev.fullname" . }}
  labels:
    {{- include "mysql-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3306
      protocol: TCP
      name: mysql
  selector:
    {{- include "mysql-dev.selectorLabels" . | nindent 4 }}
