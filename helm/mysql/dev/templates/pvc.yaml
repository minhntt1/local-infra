apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mysql-dev.fullname" . }}-data
  labels:
    {{- include "mysql-dev.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  storageClassName: {{ .Values.persistence.storageClass }}
