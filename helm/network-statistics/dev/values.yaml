replicaCount: 0

image:
  repository: ghcr.io/minhntt1/network-statistics
  tag: 9318e8c
  pullPolicy: IfNotPresent

imagePullSecret:
  enabled: false  # GHCR pull secret created out-of-band in the cluster (kubectl); not stored in git (GitHub push protection blocks it)
  registry: ghcr.io
  username: minhntt1
  password: PFNFVF9NRQ==  # TODO: replace out-of-band; base64("<SET_ME>") placeholder only

service:
  type: ClusterIP
  executorPort: 8080
  adminPort: 8081

persistence:
  enabled: false
  size: 1Gi
  storageClass: local-path

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

database:
  useSealed: true
  host: mysql-dev.dev.svc
  port: 3306
  quartzDatabase: quartz
  quartzUsername: dev
  quartzPassword: YWJjMTIz  # base64("abc123") — replace with base64 of real password
  statisticDatabase: network_statistics
  statisticUsername: dev
  statisticPassword: YWJjMTIz  # base64("abc123") — replace with base64 of real password

ingressRoute:
  enabled: true

logging:
  volume:
    type: emptyDir
