apiVersion: v1
kind: Service
metadata:
  name: {{ include "network-statistics-prod.fullname" . }}-admin
  labels:
    {{- include "network-statistics-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.adminPort }}
      targetPort: {{ .Values.service.adminPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "network-statistics-prod.selectorLabels" . | nindent 4 }}
    role: admin