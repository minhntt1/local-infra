apiVersion: v1
kind: Service
metadata:
  name: {{ include "local-infra-prod.fullname" . }}
  labels:
    {{- include "local-infra-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "local-infra-prod.selectorLabels" . | nindent 4 }}