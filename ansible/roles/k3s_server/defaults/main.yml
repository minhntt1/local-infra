---
# Defaults for the k3s_server role.
k3s_server:
  # Extra args appended to the k3s server invocation.
  extra_args: ""
  # Flannel backend (vxlan is the flannel default).
  flannel_backend: "vxlan"